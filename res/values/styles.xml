<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="TitleFont" parent="@android:style/TextAppearance.Medium">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#2800FF</item>
    </style>

    <style name="minTitleFont" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">#00008B</item>
    </style>

    <style name="item_name" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">#00008B</item>
    </style>
    
    <style name="yello_button" parent="@android:style/Widget.Button"> 
        <!-- 水平、垂直居中 --> 
        <item name="android:gravity">center_vertical|center_horizontal</item> 
        <!-- 字体颜色 --> 
        <item name="android:textColor">#FFFFFFFF</item> 
        <!-- 指定文本阴影的颜色，需要与shadowRadius一起使用 --> 
        <item name="android:shadowColor">#FF000000</item> 
        <!-- 设置阴影横向坐标开始的位置 --> 
        <item name="android:shadowDx">0</item> 
        <!-- 设置阴影纵向坐标开始的位置 --> 
        <item name="android:shadowDy">-1</item> 
        <!-- 设置阴影的半径，设置为0.1就变成文本的颜色了 --> 
        <item name="android:shadowRadius">0.2</item> 
        <item name="android:textSize">16sp</item> 
        <item name="android:textStyle">bold</item> 
        <!-- 这里引用定义的按钮样式 --> 
        <item name="android:background">@drawable/custom_button</item> 
        <item name="android:focusable">true</item> 
        <item name="android:clickable">true</item> 
    </style> 

</resources>